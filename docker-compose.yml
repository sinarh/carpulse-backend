services:
  db:
    image: postgres:16
    container_name: carpulse_postgres
    environment:
      POSTGRES_USER: carpulse
      POSTGRES_PASSWORD: carpulse
      POSTGRES_DB: carpulse
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
